---
sidebar_position: 3
hide_table_of_contents: true
---
# API

## ResizableProps

This window embeds all available properties of Resizable. it actually documents this entire library definitions.

:::tip API

Play around, and use the search. it's really easy.

You can also explore the types using your favorite IDE, this library types are exposed and very useful.

:::

:::note links

You can open links in new tab(Ctrl+click) for full screen view.
To view source code, open in new tab (GitHub can't be viewed in iframe).

:::

import useDocusaurusContext from "@docusaurus/useDocusaurusContext";

export const ApiFrame = () => {
  const { siteConfig } = useDocusaurusContext()
  const iframeRef = React.useRef(null);
  /**
   const iDoc = iframeRef.current?.contentWindow.document;
   React.useEffect(() => {
    if(iDoc){
      const iHead = iDoc.head;
      const base = iDoc.createElement("base");
      base.setAttribute("target", "_blank");
      iHead.appendChild(base)
    }
  },[iframeRef.current])
   **/
  return (
    <iframe
      ref={iframeRef}
      style={{ height: "100vh", width: "100%" }}
      src={`${siteConfig.baseUrl}typeDocs/interfaces/react_true_resizable_src_Resizable.ResizableProps.html`} />
  )
}

<ApiFrame />

