---
sidebar_position: 3
---

there are few components type this lib implements:
- UI components, have representation in DOM.
- context components, have no representation in DOM, and provide shared context for the UI components. this can be used
to customize the UI components, or to provide data for the UI components.
- State altering components, that are context components themselves. they can be used to alter the state of the UI components.